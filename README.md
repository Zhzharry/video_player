# 播放器（小程序）

一个简洁的桌面视频播放器，支持：

- 打开本地视频文件
- 进度条拖动精确定位
- 键盘左右键快进/后退固定时长
- 快速跳转到指定“分钟:秒”

## 安装

在该目录下执行：

```bash
pip install -r requirements.txt
```

## 运行

```bash
python main.py
```

## 快捷键

- `O`：打开文件
- `Space`：播放/暂停
- `←`：后退 5 秒
- `→`：前进 5 秒
- `Ctrl+G`：跳转到指定时间（mm:ss 或 m:ss）

